akka {
  loggers  = [akka.event.slf4j.Slf4jLogger]
  loglevel = debug

  actor {
    debug {
      lifecycle = on
      unhandled = on
    }
  }
}

web-stats {
  session-tracker {
    inactivity-timeout = 20 seconds
  }
}
